# git-crypt has been retired from this repo. Nix-eval secrets live at
# ~/.config/nixos-secrets/secrets.json (rendered from 1Password by
# `just render-secrets`); the signature/initials PNGs that used to live
# here as encrypted blobs are now 1Password Document items and get
# fetched by `just fetch-signatures`. See extras/docs/secrets.md.
